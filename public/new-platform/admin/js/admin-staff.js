/**
 * صفحة الكوادر الأكاديمية — الرسم مخفي حتى توفر بيانات API
 */
(function () {
    var emptyEl = document.getElementById('staff-chart-empty');
    var canvas = document.getElementById('chart-staff-diplomas');
    if (!emptyEl || !canvas || typeof Chart === 'undefined') return;

    /* عند الربط: أخفِ .dash-chart-empty وأظهر canvas ثم أنشئ Chart هنا */
})();
